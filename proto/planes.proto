syntax = "proto3";
package planes;

service Planes {
    rpc ListPlanes(Empty) returns (PlaneList);
}

message Empty {}

message PlaneList {
    repeated PlaneRead planes = 1;
}

message PlaneRead {
    string id = 1;
    string name = 2;
    int32 capacity = 3;
}
